name: CD

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write
  attestations: write
  id-token: write

jobs:
  docker-publish:
    name: Publish multi-platform image
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: production
    outputs:
      image-ref: ghcr.io/${{ github.repository }}/food-diary-app:git-${{ github.sha }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/food-diary-app
          tags: |
            type=ref,event=branch
            type=ref,event=tag
            type=semver,pattern={{version}}
            type=sha
            type=raw,value=git-${{ github.sha }}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and publish image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: true

  migrate-release:
    name: Run release migration command
    runs-on: ubuntu-latest
    needs: docker-publish

    steps:
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull published image
        run: docker pull ${{ needs.docker-publish.outputs.image-ref }}

      - name: Run migration from published image
        run: >
          docker run --rm
          -e APP_ENV=release
          -e RELEASE_VERSION=${{ github.sha }}
          -e JWT_SECRET=release-secret
          -e DB_PATH=/tmp/app.db
          -e AUTO_MIGRATE_ON_BOOT=false
          -e SEED_DEMO_DATA=false
          ${{ needs.docker-publish.outputs.image-ref }}
          node server/src/cli.js migrate

  deploy-render:
    name: Trigger Render deploy
    runs-on: ubuntu-latest
    needs:
      - docker-publish
      - migrate-release
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Trigger Render deploy hook
        run: |
          if [ -z "$RENDER_DEPLOY_HOOK_URL" ]; then
            echo "RENDER_DEPLOY_HOOK_URL is not configured. Skipping Render deploy."
            exit 0
          fi

          curl -fsSL -X POST "$RENDER_DEPLOY_HOOK_URL"
        env:
          RENDER_DEPLOY_HOOK_URL: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
